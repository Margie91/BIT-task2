import React from 'react';

const ReportComponent = (props) => {

    const { id, candidateId, candidateName, companyId,
        companyName, interviewDate, phase, status, note } = props.reports;

    return (
        <div>
            <p></p>
        </div>   
    )
}

export default ReportComponent;